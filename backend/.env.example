# Backend environment variables

# Server
PORT=4000
NODE_ENV=development

# MongoDB connection
# For local dev, ensure MongoDB is running locally or use Docker compose
MONGODB_URI=mongodb://127.0.0.1:27017/hotel_booking

# CORS allowed origins (comma-separated)
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000

# JWT
JWT_SECRET=change_me_in_production
JWT_EXPIRES_IN=7d
PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/hotel_booking
JWT_SECRET=replace-with-a-strong-secret
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:5173
# Server
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# MongoDB
MONGODB_URI=mongodb://mongo:27017/hotel_booking

# Auth
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRES_IN=7d
# Admin signup invite code (required to enable /auth/register-admin). Change to a strong secret and keep private.
ADMIN_SIGNUP_CODE=CHANGE_ME_ADMIN_CODE
